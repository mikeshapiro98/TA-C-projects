
@{
    ViewBag.Title = "Quote";
}

@using insuranceRedo3.ViewModels
@model quoteVm

<div class="row no-gutters m-3">

    <div class="col-lg-12 mx-auto shadow">

        <div class="card-header text-center  bg-primary">
            <h1 class="m-0>">Your Quote</h1>
        </div>
        <div class="card-body">
            <div class="col-10 m-2">
                    <h2>Quote</h2>
                @Model.FirstName
                @Model.LastName
                @Model.Quote

                </div>
            </div>
           
           <div id="finishQuote">

           </div> 

        </div>
</div>


@*@foreach(var qvm in Model)
    {
        @qvm.Quote;
    }*@


